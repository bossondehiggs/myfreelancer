<section class="section">
    <div class="container">
        <p class="title">Gestión de Mis Compras</p>

        <table class="table is-fullwidth">
            <thead>
                <tr>
                    <th>Fecha</th>
                    <th>Nombre del vendedor</th>
                    <th>Título</th>
                    <th>Fecha</th>
                    <th>Precio</th>
                    <th>Estado</th>
                    <th>Acción</th>
                </tr>
            </thead>
            <tbody>
            <% if @orders.blank? %>
            <tr>
                <td colspan="7" class="has-text-centered"><h1>NO HAY NADA PARA MOSTRAR</h1></td>
            </tr>
              
            <% end %>
                
                <% @orders.each do |o| %>
                <tr>
                    <td><%= o.created_at.strftime('%d %b %Y') %></td>
                    <td><%= o.seller_name%></td>
                    <td>
                        <%= link_to o.title, gig_path(o.gig) if !o.gig.nil? %>
                            <%= link_to o.title, request_path(o.request) if !o.request.nil? %>
                        
                        </td>
                    <td><%= o.due_date.strftime('%d %b %Y')%></td>
                    <td><%= o.amount%></td>
                    <td>
                        <span class="tag <%= 'is-warning' if o.procesando? %> <%= 'is-succes' if o.completado? %>">
                        <%= o.status.capitalize %>
                        </span>
                    </td>
                    <td>
                        <%= link_to 'Marcar como completada', complete_order_path(o), method: :put, class: "button is-small is-primary #{'is-hidden' if o.completado? }" %>
                    </td>

                </tr>
                <% end %>
            </tbody>
        </table>

    </div>
</section>
  