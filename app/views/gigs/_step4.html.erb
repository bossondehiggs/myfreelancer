<div class="step-content <%= 'is-active' if @step == 4 %>">
    <div class="field">
        <label for="" class="label">Url de Youtube</label>
        <%= f.text_field :video, autocomplete: "video url", class: "input" %>
    </div>

    <div class="field">
        <label for="" class="label">Añade fotos a la Galería de tu Colaboración</label>
        <div class="control">
            <div class="m-b-20">
             <!-- Photos drop zone here -->
            </div>

            <div class="columns is-multiline" style: widht: 100%>
            <!-- Uploaded photos are here -->
            
            </div>
        </div>
    </div>
</div>